pluginManagement {
    repositories {
        maven {
            url "https://maven.google.com"
        }
        gradlePluginPortal()
    }
}

include ":app"

includeBuild "project-application-plugin"

rootProject.name = "passit"